#  LLDB

```
(lldb) settings set target.language swift

(lldb) e feedbackLabel.backgroundColor = .green

(lldb) e userTextField.text = "marcello"
(lldb) e passwordTextField.text = "123"
(lldb) e loginButtonWasPressed(button)

(lldb) e CATransaction.flush()
```
